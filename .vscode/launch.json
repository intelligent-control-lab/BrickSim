{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach IsaacSim",
            "type": "debugpy",
            "request": "attach",
            "processId": "${command:pickProcess}",
            "justMyCode": false,
        }
    ]
}
